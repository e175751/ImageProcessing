import os

def main():
    pcount += 1
           px = read(mouseData.getPos())
           print(px)
           pxList.append(px)
           if count == 2:
               count = 0
               cv2.rectangle(read, pxList[0], pxList[1], (255, 212, 0))
               pxList=[]rint(os.sys.path)

if __name__ == "__main__":
    main()

